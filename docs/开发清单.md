# 开发清单

## 使用规则

- 本清单是项目开发进度的唯一记录来源。
- 每次实现、调整范围或完成验收后，必须同步更新“状态”“进度”和“验收依据”。
- 进度表示已完成的可验证工作比例，不以文档、讨论或预估替代实际实现。

状态定义：`未开始`、`进行中`、`阻塞`、`已完成`、`规划中`、`暂停`。`暂停`表示用户明确暂缓该项，保留当前进度和验收证据，后续恢复时从现有进度继续。

## 当前开发计划

| 编号 | 开发项目 | 状态 | 进度 | 前置条件 | 验收依据 |
| --- | --- | --- | ---: | --- | --- |
| D-001 | 平台架构与模块边界设计 | 已完成 | 100% | 无 | `工具平台设计.md` 已明确平台壳、工具隔离、能力模型与扩展边界。 |
| D-002 | UI Token、主题与组件规范设计 | 已完成 | 100% | 无 | `用户界面设计规范.md` 已定义 token 分层、主题和组件准则。 |
| D-003 | 项目工程初始化与目录治理 | 已完成 | 100% | 无 | 已建立 pnpm workspace、`apps/desktop` 与 `packages/config`；根级开发依赖直接声明 TypeScript，`pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build`、`pnpm format:check` 及 `cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml` 复验通过。Prettier 已按仓库 CRLF 基线配置。已实际确认 `allmytools-desktop.exe` 启动且 Vite 监听 `127.0.0.1:1420`。 |
| D-004 | 平台契约包 | 已完成 | 100% | D-003 | 已建立 `packages/platform-contracts`，公开工具清单、分类、能力、生命周期和版本兼容契约；运行时清单校验拒绝无效 ID/版本/能力及重复能力。`pnpm --filter @allmytools/platform-contracts typecheck`、`lint`、`test`（7 项）均通过。 |
| D-005 | 设计 Token 包与浅色/深色主题 | 已完成 | 100% | D-003 | 已建立 `packages/design-tokens`，提供原始、语义和组件 Token 及 `theme.css`；桌面启动页支持浅色/深色切换并通过 `aria-pressed` 暴露状态。Token 包类型检查、静态检查和 2 项测试通过，桌面主题切换测试通过。 |
| D-006 | 公用 UI 组件库 | 已完成 | 100% | D-005 | 已建立 `packages/ui`，提供 `Button`、`TextField`、`EmptyState`、`InlineMessage` 及样式入口；覆盖按钮焦点/加载/禁用、输入错误关联/禁用和空/错误内容状态。`EmptyState` 使用实例级唯一标题 ID，新增多实例关联测试；`packages/ui` 6 项组件测试及桌面入口测试通过。 |
| D-007 | 桌面应用壳与主导航 | 已完成 | 100% | D-003、D-004、D-006 | `apps/desktop/src/app` 已实现全部工具、学习、娱乐、工具和设置导航；支持工具名称/描述/关键词搜索、收藏切换、最近使用更新和浅色/深色主题切换。桌面壳层测试 8 项通过，覆盖工作区状态重新挂载恢复；`pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build`、`pnpm format:check` 和 `git diff --check` 复验通过；根目录 `启动AllMyTools.bat` 已验证能启动 Tauri/Vite，端口冲突时给出提示，并在无 pnpm 时通过 npm 临时运行兼容版本。 |
| D-008 | 多窗口、托盘与快捷键基础能力 | 已完成 | 100% | D-007 | 已接入 `tauri-plugin-window-state` 恢复主窗口状态；关闭窗口后驻留托盘，托盘左键及菜单可显示/隐藏窗口，菜单提供显式退出；设置页经用户勾选注册 `CommandOrControl+Shift+Space`，冲突和非桌面宿主均有文字提示。桌面壳额外持久化并恢复主题、主视图、分类和搜索条件，不恢复活动工具会话；`cargo check`、桌面类型检查、静态检查、测试和构建通过。 |
| D-009 | 工具注册、懒加载与故障隔离 | 已完成 | 100% | D-004、D-007 | 已新增 `packages/platform-services` 的 `ToolRegistry`，执行清单校验、重复 ID 拒绝、平台版本和能力检查、动态加载、生命周期关闭及结构化失败结果；桌面壳已注册学习/娱乐/工具三个最小内置工具，使用动态 `import()` 和 React 错误边界隔离加载/渲染故障。注册器测试 2 项、桌面交互测试 4 项，以及全 workspace 类型检查、静态检查、测试、构建、格式检查和 `cargo check` 均通过。 |
| D-010 | 开发者设计系统查看器 | 已完成 | 100% | D-005、D-006、D-007 | 设置页已接入 `DesignSystemViewer`，直接读取 `resolveThemeTokens()` 展示原始/语义/组件 Token、当前主题解析值和 Token 筛选；提供浅色/深色语义对比、Token 名称和值复制、Button/TextField/InlineMessage 状态预览。桌面测试覆盖查看器入口和真实 Token 表头，类型检查、静态检查、测试、构建、格式检查和 `cargo check` 通过。 |
| D-011 | 学习模块最小验证工具 | 已完成 | 100% | D-009 | `modules/learning/note-review` 已通过平台注册器动态加载并进入独立工具工作区；工具自己的 `src/storage.ts` 使用 `learning.note-review.draft` 命名空间，显式保存草稿并复用公共 `TextField`、`Button`，关闭时释放工具会话，壳层错误边界隔离异常。学习存储单元测试和桌面打开/保存/命名空间测试通过；全 workspace 类型检查、静态检查、测试、构建、格式检查和 `cargo check` 通过。 |
| D-012 | 娱乐模块最小验证工具 | 已完成 | 100% | D-009 | `modules/entertainment/session-picker` 已通过平台注册器动态加载并进入独立工具工作区；工具使用 `entertainment.session-picker.selection` 独立命名空间，选择仅更新会话状态，点击“保存选择”后才持久化，并在再次进入时恢复已保存选择。工具复用公共 `Button`，以 `aria-pressed` 和状态文本表达选择/保存结果；桌面壳交互测试覆盖打开、选择、保存和命名空间隔离，工具存储单元测试通过。全 workspace 类型检查、静态检查、测试、构建、格式检查、`git diff --check` 和 `cargo check` 均通过。 |
| D-013 | 工具模块最小验证工具 | 已完成 | 100% | D-009 | `modules/tools/text-workbench` 已通过平台注册器动态加载并进入独立工具工作区；工具支持清理文本两端空白，处理仅更新会话状态，点击“保存文本”后才写入 `tools.text-workbench.draft` 独立命名空间，并在再次进入时恢复已保存文本。工具复用公共 `TextField`、`Button`，空内容时保存按钮禁用，保存成功以状态文本反馈；桌面壳交互测试覆盖打开、清理、保存和命名空间隔离，工具存储单元测试通过。全 workspace 类型检查、静态检查、测试、构建、格式检查、`git diff --check` 和 `cargo check` 均通过。 |
| D-014 | 文件、网络、摄像头与系统命令能力中介 | 已完成 | 100% | D-004、D-009 | `packages/platform-services` 已提供类型化 `CapabilityBroker`：按清单声明、系统可用、用户同意和执行操作的顺序中介受限访问，并支持按工具 ID 撤销授权。未声明、用户拒绝、用户取消、系统不可用和操作异常均返回结构化失败，且不会执行受保护操作；8 项平台服务测试覆盖授权复用和全部失败分支。当前 Tauri 能力清单仍只授予窗口/快捷键权限，未提前开放文件、网络、摄像头或系统命令；首个实际工具接入前必须记录数据范围、授权文案和 Windows/macOS 降级行为。全 workspace 类型检查、静态检查、测试、构建、格式检查、`git diff --check` 和 `cargo check` 均通过。 |
| D-015 | Windows/macOS 质量、打包与发布基础 | 暂停 | 45% | D-008 至 D-014 | 已完成 Windows release 可执行文件构建和 `pnpm release:check` 发布预检。完整 MSI 尚未生成：Tauri/WiX 下载与 `winget download` 均在当前网络超时，`winget install` 还要求管理员权限启用 .NET Framework 3.5；当前宿主仅有 Windows Rust 目标，未验证 macOS `.app`/DMG、Windows/MSI 安装卸载、两端签名及 macOS 公证。恢复后从 45% 继续。 |
| D-016 | 账号与云端同步 | 暂停 | 0% | 确定具体同步数据和隐私方案 | 尚未开始实现；恢复前需要先确定具体同步数据、数据模型、冲突策略、离线行为和安全凭据方案。恢复后从 0% 开始。 |
| D-017 | 第三方插件 SDK 与签名加载 | 暂停 | 0% | D-004、D-014、D-015 | 尚未开始实现；恢复前需要先完成插件签名、隔离、权限撤销和崩溃隔离设计。恢复后从 0% 开始。 |

## 下一步

已完成 D-003 至 D-014；D-015、D-016、D-017 暂停。后续恢复开发时，D-015 从 45% 继续，D-016 和 D-017 从 0% 开始。
